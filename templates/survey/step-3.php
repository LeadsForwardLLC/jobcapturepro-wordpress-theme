<?php
/**
 * Legacy contact step — retired from the public gate (kept for back-compat includes).
 * Phone / referral are collected after the demo when needed.
 *
 * @package JCP_Core
 */
// Intentionally empty: conversion gate is now a single screen (templates/survey/step-1.php).
